- name: facts
  hosts: web
  tasks:
  - name: ansible facts
    ansible.builtin.debug:
      msg: "{{ ansible_facts }}"

- name: print the facts
  hosts: web
  tasks:
  - name: print the facts
    ansible.builtin.debug:
      msg: "{{ ansible_facts }}"